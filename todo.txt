Readme
Sortable columns
Local ENV variable support

Questions:
- if someone supplies a number into a string field, do we care?
- how do we manage conflicts - handle it ourselves?

Test:
- Schema, invalid values for type
- Schema, invalid field names